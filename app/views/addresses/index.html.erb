<h1>Listing addresses</h1>

<table>
  <tr>
    <th>Street</th>
    <th>City</th>
    <th>Zip</th>
    <th>Country</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @addresses.each do |address| %>
  <tr>
    <td><%= address.street %></td>
    <td><%= address.city %></td>
    <td><%= address.zip %></td>
    <td><%= address.country %></td>
    <td><%= link_to 'Show', contact_addresses_path %></td>
    <td><%= link_to 'Edit', edit_contact_address_path(contact) %></td>
    <td><%= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Address', new_address_path %>
